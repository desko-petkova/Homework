🧩 1. Какво представляват входните данни?

Входни данни са информацията, която програмата получава, за да извърши изчисления или да вземе решения.

Примери:
Въвеждане на число от клавиатурата – напр. възраст на потребителя.
Въвеждане на текст – напр. име.

В C# най-често се използва:

	string name = Console.ReadLine();     // въвеждане на текст
	int age = int.Parse(Console.ReadLine()); // въвеждане на цяло число

⚙️ 2. Какво са спомагателни променливи?

Спомагателни променливи са променливи, които не са част от основните входни или изходни данни, но се използват за:

- съхраняване на междинни резултати;
- извършване на изчисления;
- улесняване на логиката в програмата.

Пример:
Да намерим средното аритметично на две числа:

	int a = int.Parse(Console.ReadLine());
	int b = int.Parse(Console.ReadLine());
	int sum = a + b;         // спомагателна променлива
	double average = sum / 2.0;  //Внимание!!!  делим sum на 2.0 за да получим дробен резултат
	Console.WriteLine(average);

## Тук sum е спомагателна променлива, използвана само за улеснение при изчислението.

🔢 3. Типове данни в C#

В C# всеки тип данни определя каква информация може да се съхранява в променливата и колко памет заема.

Тип данни	Пример	Описание
int		5	Цяло число (–2 147 483 648 до 2 147 483 647)
double		3.14	Число с плаваща запетая (реално число)
float		2.5f	Реално число, но с по-малка точност
decimal		12.99m	Висока точност за пари и финанси
char		'A'	Един символ
string		"Hello"	Текстова последователност
bool		true / false	Булева стойност (логическа)

🧮 4. Примерна задача

Да се въведат две цели числа и да се изведе тяхната сума и средно аритметично.

Решение:

using System;

class Program
{
    static void Main()
    {
        Console.Write("Въведете първото число: ");
        int a = int.Parse(Console.ReadLine());

        Console.Write("Въведете второто число: ");
        int b = int.Parse(Console.ReadLine());

        int sum = a + b;             // спомагателна променлива
        double average = sum / 2.0;  // спомагателна променлива

        Console.WriteLine($"Сума: {sum}");
        Console.WriteLine($"Средно аритметично: {average}");
    }
}

## Анализ на примера:

Входни данни: a, b
Спомагателни променливи: sum, average
Типове данни:	
		int – за цели числа
		double – за реално число (средно аритметично)